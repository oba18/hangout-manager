<%= form_with(model: hang_out, local: true) do |form| %>
  <% if hang_out.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hang_out.errors.count, "error") %> prohibited this hang_out from being saved:</h2>

      <ul>
        <% hang_out.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.time_select :start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.time_select :end_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
